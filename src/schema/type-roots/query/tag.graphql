type Query{
    "Lista de etiquetas (tags)"
    tags(page: Int = 1, itemsPage: Int = 20, active: ActiveFilterEnum = ACTIVE): ResultTag
    "Item del género seleccionado"
    tag(id: ID!): ResultTag
}